{
  "name": "devjobs",
  "private": true,
  "workspaces": [
    "api",
    "frontend",
    "scripts"
  ],
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "arktype": "^2.1.16"
  },
  "scripts": {
    "dev": "concurrently -n api,front \"bun run --cwd api dev\" \"bun run --cwd frontend dev\"",
    "test": "bun test api && bun test frontend && bun test scripts",
    "build": "bun run --cwd api build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
