{
  "name": "devjobs",
  "private": true,
  "workspaces": [
    "api",
    "frontend",
    "scripts"
  ],
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "arktype": "^2",
    "playwright": "^1.52.0"
  },
  "scripts": {
    "dev": "concurrently -n api,front \"bun run --cwd api dev\" \"bun run --cwd frontend dev\"",
    "test": "bun test api && bun test frontend && bun test scripts",
    "build": "bun run --cwd api build",
    "check": "bun run --cwd api check && bun run --cwd frontend lint && bun run --cwd scripts check",
    "format": "bun run --cwd scripts format"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "biome": "^0.3.3",
    "concurrently": "^9.1.2"
  }
}
