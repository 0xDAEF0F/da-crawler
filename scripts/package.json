{
  "name": "scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "format": "biome format --write"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
